/**
 * MetricView component styles
 */

import { chartBaseStyles } from "./base";

export const metricViewStyles = {
  container: chartBaseStyles.centeredContainer,
  value: chartBaseStyles.metricValue,
  label: chartBaseStyles.metricLabel,
  noDataValue: chartBaseStyles.noDataValue,
  noDataLabel: chartBaseStyles.noDataLabel,
};
